---
# Per environment values which override defaults in hmpps-person-record-manage-ui/values.yaml

generic-service:
  ingress:
    host: hmpps-person-record-manage-ui.hmpps.service.justice.gov.uk

  env:
    INGRESS_URL: "http://localhost:3000"
    HMPPS_AUTH_URL: "https://sign-in.hmpps.service.justice.gov.uk/auth"
    TOKEN_VERIFICATION_API_URL: "https://token-verification-api.prison.service.justice.gov.uk"
    EXAMPLE_API_URL: 'https://template-kotlin.hmpps.service.justice.gov.uk'
    AUDIT_ENABLED: "false"

generic-prometheus-alerts:
  alertSeverity: hmpps-person-record-alerts-prod
